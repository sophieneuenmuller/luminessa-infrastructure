(headers) {
	header {
		# Prevent clickjacking
		X-Frame-Options "SAMEORIGIN"
		# XSS protection
		X-Content-Type-Options "nosniff"
		# Referrer policy
		Referrer-Policy "strict-origin-when-cross-origin"
		# Enforce HTTPS
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	}
}

git.luminessa.net {
	# Limit request body size to prevent large payload attacks
	request_body {
		max_size 10MB
	}

	reverse_proxy forgejo:3000

	import headers
}

blog.luminessa.net {
	root * /var/www/luminessa-blog
	file_server
	encode gzip

	import headers
}

sync.luminessa.net {
	# Limit request body size (Syncthing handles large files internally via its own protocol)
	request_body {
		max_size 10MB
	}

	reverse_proxy syncthing:8384

	import headers
}

luminessa.net, www.luminessa.net {
	root * /var/www/luminessa-landing
	file_server
	encode gzip

	import headers
}

backups.luminessa.net {
	request_body {
		max_size 10MB
	}

	reverse_proxy backrest:9898

	import headers
}

vpn.luminessa.net {
	# VPN administrative interface requirement
	reverse_proxy wg-easy:51821

	import headers
}

libros.luminessa.net {
	request_body {
		max_size 100MB
	}

	reverse_proxy grimmory:6060

	import headers
}

n8n.luminessa.net {
	request_body {
		max_size 10MB
	}

	reverse_proxy n8n:5678

	import headers
}

# engram.luminessa.net {
# 	request_body {
# 		max_size 20MB
# 	}

# 	reverse_proxy engram-cloud:18080

# 	import headers
# }

uptime.luminessa.net {
	request_body {
		max_size 10MB
	}

	reverse_proxy uptime-kuma:3001

	import headers
}
